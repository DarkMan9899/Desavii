/**
 * SceneKit — the SVG scene foundation for future TOP ambient scenes
 * (design-tooling setup, brief step 6). Not wired into any of the 9
 * existing category environments in this step — see `useParallaxPointer.js`
 * and `SceneStage.jsx`'s own header comments for why.
 */

export { default as SceneStage } from './SceneStage.jsx';
export { default as useParallaxPointer } from './useParallaxPointer.js';
export * from './primitives/index.js';
