/** Mirrors `apps/api/src/modules/advertising/services/advertisementService.js`'s `PLACEMENT_CODES` — the only two placements Sprint E activates. */
export const PLACEMENT_CODES = Object.freeze({
  HOME: 'HOMEPAGE_SECTION',
  CATEGORY: 'CATEGORY_TOP',
});

export default PLACEMENT_CODES;
