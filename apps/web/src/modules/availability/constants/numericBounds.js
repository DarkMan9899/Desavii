/**
 * `INT UNSIGNED` column ceiling — mirrors
 * `apps/api/src/validation/sqlIntegerBounds.js`, which caps unit capacity
 * and calendar block/external-reservation quantities at this value.
 */

export const INT_UNSIGNED_MAX = 4294967295;

export default INT_UNSIGNED_MAX;
